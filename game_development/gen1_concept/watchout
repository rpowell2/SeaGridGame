One additional note after reviewing your networking code: your current recv_json() implementation is vulnerable to partial TCP packet reads because TCP is a stream, not a message protocol. Before adding gameplay systems, I'd actually prioritize replacing the networking layer with newline-buffered packet processing. That change will save you a tremendous amount of debugging later as player counts increase.
